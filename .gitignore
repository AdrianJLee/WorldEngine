# Directories
.vs/
bin/
bin-int/

# Files
*.user
build
*.vcxproj
*.filters
.github
.codex
.vscode
docs
tools
AGENTS.md
Editor/wui-layout.json
Editor/wui-layout.json.bak
Editor/wui-browser.json
# 材质单测的沙箱目录(World.Material 在其中写临时 .wmat;失败时可能留下残件)
Game/material_tests_tmp/
# 内容浏览器"New Material"测试产生的临时材质目录(内容根是 Game/assets,这里是测试残留)
Game/materials/

# W8:编辑器脚本工作区脚手架(create-if-missing 生成;用户改过的配置不入库即保留)。
# 上面的 `.vscode` 忽略规则会连带排除父目录,必须先把目录本身取消忽略,再放行其内容。
!Game/.vscode/
!Game/.vscode/**
!Game/.luau-lsp/
!Game/.luau-lsp/**
